# Core dependencies for perturbation transformer model
torch>=2.0.0
lightning>=2.0.0
geomloss>=0.2.4
wandb>=0.15.0
line-profiler

# Data processing
numpy>=1.21.0
pandas>=1.3.0
scipy>=1.7.0

# For the protoplast data module (adjust version as needed)
# protoplast

# Optional: for better performance
torch-audio>=2.0.0
torch-vision>=0.15.0
